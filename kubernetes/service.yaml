apiVersion: v1
kind: Service
metadata:
  name: esports-realtime-service
spec:
  selector:
    app: esports-realtime
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer